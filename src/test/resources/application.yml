spring:
  datasource:
    url: jdbc:h2:mem:test
    username: sa
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: db.changelog/db.changelog-test.xml
  sql:
    init:
      mode: never